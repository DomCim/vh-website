import React from 'react'

import { BelegAnsicht } from './Ansicht'

/** Hülle ohne Datenzugriff — der Beleg kommt aus dem Bestand im Gerät. */
export default function BelegSeite() {
  return <BelegAnsicht />
}
